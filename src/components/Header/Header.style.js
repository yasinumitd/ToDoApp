import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header:{
        padding:10,
        flexDirection:"row",
        justifyContent: "space-between"
      },
      headerText:{
        fontSize:25,
        fontWeight:'bold',
      },
      countText:{
        fontSize:25,
        fontWeight:'bold',
        marginRight:5
      },
})